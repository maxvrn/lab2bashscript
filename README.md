# PyProject Bootstrap

**Автоматический инициализатор Python-проектов**

## 📋 Описание
`PyProject Bootstrap` — это bash-скрипт для разработчиков, который автоматизирует рутинную задачу по созданию базовой структуры нового Python-проекта. Скрипт создает стандартизированную директорию, инициализирует изолированное виртуальное окружение с указанной версией Python и генерирует все необходимые стартовые файлы (`requirements.txt`, `.gitignore`, `README.md`), позволяя сосредоточиться на коде, а не на настройке.

Проект создан для ускорения и унификации процесса начала работы над новыми приложениями или библиотеками на Python.

## ✨ Основные возможности
*   **Автоматическое создание структуры каталогов**: генерирует стандартные папки (`src/`, `tests/`, `docs/` и др.) для организованного хранения кода.
*   **Управление версиями Python**: создает виртуальное окружение с использованием конкретной версии интерпретатора (например, 3.11, 3.12).
*   **Генерация базовых файлов**: предоставляет готовые шаблоны для `README.md`, `requirements.txt`, `.gitignore` и основного скрипта.
*   **Быстрый старт**: одна команда разворачивает полностью готовую к работе среду разработки.

## 🛠️ Технологии
*   **Bash** — язык основного скрипта.
*   **Python 3** — целевой язык создаваемых проектов.
*   **venv** — стандартный модуль Python для создания виртуальных окружений.

## 🚀 Установка и использование

### Предварительные требования
*   Операционная система **Ubuntu** (или совместимый дистрибутив Linux).
*   Установленная нужная версия Python (например, `python3.11`, `python3.12`).
*   Установленный соответствующий пакет `pythonX.Y-venv` (например, `python3.11-venv`).

### Установка скрипта
1.  Скачайте скрипт и сделайте его исполняемым:
    ```bash
    wget -O ~/create_python_project.sh <URL_СКРИПТА>
    chmod +x ~/create_python_project.sh
    ```

### Быстрый запуск (для пользователя)
Для создания нового проекта выполните команду в терминале:
```bash
~/create_python_project.sh <ИМЯ_ВАШЕГО_ПРОЕКТА> <ВЕРСИЯ_PYTHON>
```
## 🔧 Для разработчиков и адаптации
### Если вы хотите доработать или адаптировать скрипт под свои нужды:

1.  Склонируйте репозиторий проекта.
2.  Внесите изменения в файл create_python_project.sh (например, добавив новые папки или изменив шаблоны файлов).
3.  Протестируйте изменения, запуская скрипт на создание тестовых проектов.
4.  Для внесения своего вклада создайте форк репозитория и pull request с описанием изменений.

## 👥 Участники
#### Проект разработан и поддерживается в рамках учебной инициативы по освоению автоматизации процессов разработки на Python и Bash.

